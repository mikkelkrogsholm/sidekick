# OpenAI API Key
# Get your API key from: https://platform.openai.com/api-keys
# Requires access to Realtime API and Embeddings API
OPENAI_API_KEY=sk-your-api-key-here

# Server Configuration
# Port number for the Express server (default: 3000)
PORT=3000

# Embedding Configuration
# Model for text embeddings (options: text-embedding-3-large, text-embedding-3-small, text-embedding-ada-002)
EMBEDDING_MODEL=text-embedding-3-large

# Embedding strategy (currently only supports: minutely)
EMBED_STRATEGY=minutely

# How often to embed buffered transcripts in minutes (default: 3)
EMBED_INTERVAL_MINUTES=3

# Database Configuration
# Path to SQLite database file for storing transcripts and embeddings
SQLITE_DB_PATH=./data/transcripts.db